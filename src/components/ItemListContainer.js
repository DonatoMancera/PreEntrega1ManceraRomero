const ItemListContainer = ({greeting}) =>{
    return (
        <div>
            <h1 className="title is-primary has-text-centered">{greeting}</h1>
        </div>
    );
}

export default ItemListContainer;